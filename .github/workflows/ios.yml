name: iOS CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PROJECT_PATH: Boilerplate/Boilerplate.xcodeproj
  SCHEME: Boilerplate

jobs:
  build-and-test:
    name: Build & Test
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -switch /Applications/Xcode_16.app/Contents/Developer

      - name: Show environment
        run: |
          xcodebuild -version
          swift --version

      - name: Register simulator runtimes
        run: |
          # A freshly-selected Xcode install needs its first-launch step to run
          # before any concrete simulators show up in `simctl`/`-showdestinations`
          # (otherwise only the "Any iOS Simulator Device" placeholder is listed).
          sudo xcodebuild -runFirstLaunch
          xcrun simctl list runtimes
          xcrun simctl list devices available

      - name: Resolve simulator destination
        id: destination
        run: |
          DEVICE=$(xcrun simctl list devices available \
            | grep -m 1 -E '^\s+iPhone' \
            | sed -E 's/^[[:space:]]+//; s/ \(.*$//')
          if [ -z "$DEVICE" ]; then
            echo "No iPhone simulator found; falling back to a generic destination." >&2
            echo "generic=true" >> "$GITHUB_OUTPUT"
          else
            echo "device=$DEVICE" >> "$GITHUB_OUTPUT"
            echo "generic=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build
        run: |
          set -o pipefail
          if [ "${{ steps.destination.outputs.generic }}" = "true" ]; then
            DESTINATION='generic/platform=iOS Simulator'
          else
            DESTINATION="platform=iOS Simulator,name=${{ steps.destination.outputs.device }}"
          fi
          xcodebuild build \
            -project "$PROJECT_PATH" \
            -scheme "$SCHEME" \
            -destination "$DESTINATION" \
            -configuration Debug \
            CODE_SIGNING_ALLOWED=NO

      - name: Test
        run: |
          set -o pipefail
          xcodebuild test \
            -project "$PROJECT_PATH" \
            -scheme "$SCHEME" \
            -destination "platform=iOS Simulator,name=${{ steps.destination.outputs.device }}" \
            -configuration Debug \
            -resultBundlePath TestResults.xcresult \
            CODE_SIGNING_ALLOWED=NO

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: TestResults.xcresult
          retention-days: 7

  lint:
    name: SwiftLint
    runs-on: macos-15
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint
        run: swiftlint lint --strict --quiet || true
